apiVersion: v1
kind: Service
metadata:
  name: {{ template "programrepositoryapi.fullname" . }}
  labels:
    app: {{ template "programrepositoryapi.name" . }}
    chart: {{ template "programrepositoryapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "programrepositoryapi.name" . }}
    release: {{ .Release.Name }}
