apiVersion: v1
kind: Service
metadata:
  name: {{ template "communication.fullname" . }}
  labels:
    app: {{ template "communication.name" . }}
    chart: {{ template "communication.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "communication.name" . }}
    release: {{ .Release.Name }}
