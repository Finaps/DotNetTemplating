apiVersion: v1
kind: Service
metadata:
  name: {{ template "microservicestemplate.fullname" . }}
  labels:
    app: {{ template "microservicestemplate.name" . }}
    chart: {{ template "microservicestemplate.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "microservicestemplate.name" . }}
    release: {{ .Release.Name }}
